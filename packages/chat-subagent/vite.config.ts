import react from "@vitejs/plugin-react"; import { defineConfig } from "vite"; import { dashboardClientUrl as client } from "../dashboard/client-version"; export default defineConfig({ plugins: [react()], resolve: { alias: [{ find: "react/jsx-runtime", replacement: client }, { find: "react", replacement: client }] }, build: { outDir: "dist/frontend", rollupOptions: { external: [client] }, lib: { entry: "frontend/index.tsx", formats: ["es"], fileName: () => "chat-subagent.js" } } });
