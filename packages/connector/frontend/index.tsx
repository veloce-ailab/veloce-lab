import type { DashboardContext } from "@velocelab/dashboard/frontend"; import AdvancedChatDevices, { AdvancedChatDeviceDetail } from "./pages/AdvancedChatDevices"; import { Laptop } from "lucide-react";
export function apply(ctx: DashboardContext) { ctx.page({ frame: "settings", path: "/settings/devices", component: AdvancedChatDevices }); ctx.page({ frame: "settings", path: "/settings/devices/:id", component: AdvancedChatDeviceDetail }); ctx.nav({ id: "advanced-chat-devices", label: "设备管理", path: "/settings/devices", icon: Laptop, order: 30, scope: "settings", group: "chat" }); }
export default apply
