{
  "name": "@velocelab/backend",
  "private": true,
  "type": "module",
  "packageManager": "yarn@4.14.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -Apt run build",
    "dev": "yumeri start",
    "start": "yumeri start"
  },
  "dependencies": {
    "yumeri": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "typescript": "^5.9.2"
  }
}
