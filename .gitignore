flai.db
flai.exe
flai.exe~
/dist/
/docs/
/ref/
# Desktop Electron app is part of the repository.
/data/
.env
.jwt_secret
*.db-shm
*.db-wal
log/
/original/
/old/
transfer.md
/.cache/
node_modules/
.yarn/install-state.gz
.tmp-koishi-webui/
dist/
**/dist/
.dsh-tmp/
# The deployment's own configuration. Untracked on purpose: it holds this
# machine's port, database path and (if auth is on) the administrator's
# credentials. scripts/yumeri.json is the tracked template; the installer copies
# it into place. Leading slash so the template stays tracked.
/yumeri.json
