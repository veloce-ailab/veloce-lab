import { Ionicons } from "@expo/vector-icons"
import { useCallback, useEffect, useRef, useState } from "react"
import { ActivityIndicator, Modal, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from "react-native"
import { request } from "../api"
import type { Palette } from "../theme"
import { Action, Input, Sheet, Top } from "./TasksPage"

type GroupDraft = { name: string; description: string; connector_device_id: string; connector_workspace_path: string; agent_ids: string[] }

export default function GroupChatPage({ colors, groupID, onBack }: { colors: Palette; groupID: string; onBack: () => void }) {
  const [detail, setDetail] = useState<any>(null)
  const [agents, setAgents] = useState<any[]>([])
  const [draft, setDraft] = useState("")
  const [form, setForm] = useState<GroupDraft>({ name: "", description: "", connector_device_id: "", connector_workspace_path: "", agent_ids: [] })
  const [sending, setSending] = useState(false)
  const [saving, setSaving] = useState(false)
  const [settingsOpen, setSettingsOpen] = useState(false)
  const [error, setError] = useState("")
  const scroll = useRef<ScrollView>(null)

  const load = useCallback(async () => { try { const [nextDetail, nextAgents] = await Promise.all([request<any>(`/user/advanced-chat/chat-groups/${encodeURIComponent(groupID)}`), request<any[]>("/user/advanced-chat/agents")]); const group = nextDetail?.group || nextDetail; setDetail(nextDetail); setAgents(Array.isArray(nextAgents) ? nextAgents : []); setForm({ name: group?.name || "", description: group?.description || "", connector_device_id: group?.connector_device_id || "", connector_workspace_path: group?.connector_workspace_path || "", agent_ids: (group?.members || group?.agent_ids || []).map((item: any) => typeof item === "string" ? item : item.agent_id).filter(Boolean) }) } catch (cause) { setError(message(cause)); setDetail(null) } }, [groupID])
  useEffect(() => { void load() }, [load])
  const send = async () => { if (!draft.trim() || sending) return; setSending(true); try { await request(`/user/advanced-chat/chat-groups/${encodeURIComponent(groupID)}/messages`, { method: "POST", body: { content: draft.trim(), mention_member_ids: [] } }); setDraft(""); await load() } catch (cause) { setError(message(cause)) } finally { setSending(false) } }
  const toggleMember = (id: string) => setForm(current => ({ ...current, agent_ids: current.agent_ids.includes(id) ? current.agent_ids.filter(value => value !== id) : [...current.agent_ids, id] }))
  const saveSettings = async () => { if (!form.name.trim() || !form.agent_ids.length) { setError("请填写群组名称并至少选择一个代理"); return } setSaving(true); try { await request(`/user/advanced-chat/chat-groups/${encodeURIComponent(groupID)}`, { method: "PUT", body: { name: form.name.trim(), description: form.description.trim(), connector_device_id: form.connector_device_id.trim(), connector_workspace_path: form.connector_workspace_path.trim(), agent_ids: form.agent_ids, member_configs: form.agent_ids.map(id => ({ agent_id: id, model_name: agents.find(item => item.id === id)?.default_model || "", user_channel_id: 0, connector_device_id: "" })) } }); setSettingsOpen(false); await load() } catch (cause) { setError(message(cause)) } finally { setSaving(false) } }

  const group = detail?.group || detail
  return <View style={styles.root}><Top colors={colors} title={group?.name || "群组"} onBack={onBack} right="settings" onRight={() => setSettingsOpen(true)} />{error ? <Text style={{ color: colors.destructive, padding: 12 }}>{error}</Text> : null}<ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={[styles.members, { borderBottomColor: colors.border }]}>{(group?.members || []).map((member: any) => <View key={member.id || member.agent_id} style={[styles.member, { backgroundColor: colors.input }]}><View style={[styles.dot, { backgroundColor: member.status === "working" ? "#22c55e" : colors.muted }]} /><Text style={{ color: colors.text, fontSize: 12 }}>{member.agent_name || member.agent_id}</Text></View>)}</ScrollView><ScrollView ref={scroll} style={styles.flex} contentContainerStyle={styles.messages}>{detail?.messages?.map((item: any, index: number) => <View key={item.id || index} style={[styles.message, { backgroundColor: item.sender_type === "user" ? colors.primary : colors.bubble, alignSelf: item.sender_type === "user" ? "flex-end" : "flex-start" }]}><Text style={{ color: item.sender_type === "user" ? colors.primaryText : colors.muted, fontSize: 12 }}>{item.sender_name || "成员"}</Text><Text style={{ color: item.sender_type === "user" ? colors.primaryText : colors.text, fontSize: 16, lineHeight: 23 }}>{item.content}</Text></View>) || <ActivityIndicator color={colors.muted} />}</ScrollView><View style={[styles.composer, { borderColor: colors.border, backgroundColor: colors.card }]}><TextInput value={draft} onChangeText={setDraft} multiline placeholder="发送群消息" placeholderTextColor={colors.muted} style={[styles.input, { color: colors.text, backgroundColor: colors.input }]} /><Pressable onPress={() => void send()} disabled={sending}><Ionicons name="arrow-up-circle" size={30} color={colors.text} /></Pressable></View><Modal visible={settingsOpen} transparent animationType="slide" onRequestClose={() => setSettingsOpen(false)}><Sheet colors={colors} title="群组设置" onClose={() => setSettingsOpen(false)}><ScrollView contentContainerStyle={styles.settings}><Input colors={colors} label="群组名称" value={form.name} onChangeText={(value: string) => setForm(current => ({ ...current, name: value }))} /><Input colors={colors} label="说明" value={form.description} onChangeText={(value: string) => setForm(current => ({ ...current, description: value }))} multiline /><Input colors={colors} label="连接器设备 ID" value={form.connector_device_id} onChangeText={(value: string) => setForm(current => ({ ...current, connector_device_id: value }))} /><Input colors={colors} label="工作目录" value={form.connector_workspace_path} onChangeText={(value: string) => setForm(current => ({ ...current, connector_workspace_path: value }))} /><Text style={{ color: colors.muted, fontSize: 12 }}>群组成员</Text>{agents.map(agent => <Pressable key={agent.id} onPress={() => toggleMember(agent.id)} style={[styles.memberChoice, { borderColor: colors.border, backgroundColor: form.agent_ids.includes(agent.id) ? colors.input : colors.card }]}><Ionicons name={form.agent_ids.includes(agent.id) ? "checkbox-outline" : "square-outline"} size={19} color={colors.text} /><View style={{ flex: 1 }}><Text style={{ color: colors.text }}>{agent.name}</Text><Text style={{ color: colors.muted, fontSize: 11 }}>{agent.default_model || "默认模型"}</Text></View></Pressable>)}<Action colors={colors} title={saving ? "保存中…" : "保存群组设置"} onPress={saveSettings} disabled={saving} /></ScrollView></Sheet></Modal></View>
}
function message(error: unknown) { return error instanceof Error ? error.message : "发生未知错误" }
const styles = StyleSheet.create({ root: { flex: 1 }, flex: { flex: 1 }, members: { flexDirection: "row", gap: 7, paddingHorizontal: 10, paddingVertical: 8, borderBottomWidth: StyleSheet.hairlineWidth }, member: { borderRadius: 14, paddingHorizontal: 8, paddingVertical: 5, flexDirection: "row", gap: 5, alignItems: "center" }, dot: { width: 7, height: 7, borderRadius: 4 }, messages: { padding: 14, gap: 10, flexGrow: 1 }, message: { maxWidth: "86%", borderRadius: 14, padding: 11, gap: 4 }, composer: { borderTopWidth: StyleSheet.hairlineWidth, padding: 10, flexDirection: "row", alignItems: "flex-end", gap: 8 }, input: { flex: 1, minHeight: 44, maxHeight: 120, borderRadius: 14, paddingHorizontal: 12, paddingVertical: 10, fontSize: 16 }, settings: { gap: 13, paddingBottom: 16 }, memberChoice: { minHeight: 49, borderWidth: 1, borderRadius: 9, paddingHorizontal: 10, flexDirection: "row", alignItems: "center", gap: 9 } })
